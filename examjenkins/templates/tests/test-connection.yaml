apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "examjenkins.fullname" . }}-test-connection"
  labels:
    {{- include "examjenkins.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "examjenkins.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
